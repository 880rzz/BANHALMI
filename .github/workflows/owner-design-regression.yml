name: Owner-reported design regression matrix

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  design-matrix:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Prepare production-like artifact
        run: |
          set -euo pipefail
          rm -rf _site
          mkdir -p _site
          git ls-files -z | rsync -a --from0 --files-from=- ./ _site/
          node tools/optimize-production-artifact.mjs _site
          node tools/restore-production-design-authority.mjs _site
          node tools/harden-production-artifact.mjs _site
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - name: Run full owner-reported viewport matrix
        run: |
          set -euo pipefail
          node tools/perf-static-server.mjs _site 4173 > /tmp/owner-design.log 2>&1 &
          SERVER_PID=$!
          trap 'kill $SERVER_PID || true' EXIT
          for i in $(seq 1 80); do curl -fsS http://127.0.0.1:4173/ >/dev/null && break; sleep 0.25; done
          AUDIT_BASE_URL=http://127.0.0.1:4173 AUDIT_SITE_DIR=_site node tools/audit-owner-reported-design-regressions.mjs
