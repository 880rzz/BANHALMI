name: Production audit checks
on:
  pull_request:
  push:
    branches: [codex/production-audit-fixes]
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node tools/audit-regression.mjs
