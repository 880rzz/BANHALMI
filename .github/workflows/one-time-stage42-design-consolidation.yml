name: One-time stage42 design consolidation
on:
  push:
    branches: [main]
    paths:
      - 'tools/consolidate-runtime-design-stage42.mjs'
      - '.github/workflows/one-time-stage42-design-consolidation.yml'
permissions:
  contents: write
jobs:
  consolidate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node tools/consolidate-runtime-design-stage42.mjs
      - name: Commit consolidation
        run: |
          if git diff --quiet; then
            echo 'No changes required'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add assets/js/site-config.js assets/css/accessibility-stage14.css
          git commit -m 'fix(design): move validation styles out of runtime JS'
          git push
