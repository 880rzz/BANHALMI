name: Fix BANHALMI browser QA
on:
  push:
    branches: [main]
    paths:
      - tools/fix-browser-qa-systemic-20260825.mjs
      - .github/workflows/fix-browser-qa-systemic-20260825.yml
  workflow_dispatch:
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node tools/fix-browser-qa-systemic-20260825.mjs
      - run: npm test
      - name: Commit CSS correction
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add assets/css/site.css
          if git diff --cached --quiet; then
            echo 'No CSS changes.'
          else
            git commit -m 'Fix systemic BANHALMI browser QA failures'
            git push origin HEAD:main
          fi
