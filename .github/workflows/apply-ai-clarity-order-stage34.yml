name: Apply AI clarity order stage 34

on:
  push:
    branches: [main]
    paths:
      - 'tools/apply-ai-clarity-order-stage34.mjs'
      - '.github/workflows/apply-ai-clarity-order-stage34.yml'

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply order fix
        run: node tools/apply-ai-clarity-order-stage34.mjs
      - name: Commit migration result
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix(ai): preserve H1-summary-agent entry order" || exit 0
          git push
