import{readFile,writeFile,readdir,access,unlink}from'node:fs/promises';import path from'node:path';
const root=path.resolve(process.argv[2]||'_site'),cssSrc=path.resolve('assets/css/design-authority-stage77.css'),cssDst=path.join(root,'assets/css/style.css'),oldToken='style.css?v=20260813-stage75-first-principles',newToken='style.css?v=20260814-stage77-design';
const commercial=new Set(['portrait/index.html','lifestyle/index.html','event-photography/index.html','hu/portre/index.html','hu/brand/index.html','hu/rendezvenyfotozas/index.html','de-at/portrait/index.html','de-at/brand/index.html','de-at/eventfotografie/index.html']),fineArt=new Set(['glamour/index.html','hu/muveszi-fotografia/index.html','de-at/fine-art/index.html']),stop=new Set('a an and or the of to for in on with from by at is are be as that this it its your you one egy az és vagy hogy aki ami amire mit der die das ein eine einer einen und oder von für mit ist sind zu im in den dem des'.split(/\s+/));
const exists=async f=>{try{await access(f);return true}catch{return false}},plain=s=>s.replace(/<[^>]+>/g,'').replace(/&nbsp;/g,' ').replace(/\s+/g,' ').trim(),attr=(t,n,v)=>new RegExp(`\\s${n}=["'][^"']*["']`,'i').test(t)?t.replace(new RegExp(`\\s${n}=["'][^"']*["']`,'i'),` ${n}="${v}"`):t.replace(/>$/,` ${n}="${v}">`);
if(await exists(cssDst)){let c=await readFile(cssDst,'utf8');if(!c.includes('STAGE77-DESIGN-FIRST-PRINCIPLES:START'))c+='\n'+(await readFile(cssSrc,'utf8')).replace(/\/\*[\s\S]*?\*\//g,'').replace(/\s+/g,' ').trim()+'\n';await writeFile(cssDst,c,'utf8');const extra=path.join(root,'assets/css/design-authority-stage77.css');if(await exists(extra))await unlink(extra)}
function accent(s){if(/bn-heading-accent|title-accent/.test(s))return s;const p=s.split(/(<[^>]+>)/g);for(let i=p.length-1;i>=0;i--){if(p[i].startsWith('<'))continue;const w=[...p[i].matchAll(/[\p{L}\p{N}][\p{L}\p{N}’'\-–]+/gu)],h=[...w].reverse().find(x=>!stop.has(x[0].toLowerCase())&&x[0].length>2)||w.at(-1);if(h){const e=h[0].replace(/[.*+?^${}()|[\]\\]/g,'\\$&');p[i]=p[i].replace(new RegExp(`(${e})(?![\\s\\S]*${e})`,'i'),'<span class="bn-heading-accent">$1</span>');return p.join('')}}return s}
const surface=(t,f)=>/\btrust-proof\b|\bcta-band\b|\bpresence-thesis\b/.test(t)?'dark':/\bservice-gallery-section\b|\breviews-drawer-section\b|\bnext-step-selector\b|\bfp-decision-system\b/.test(t)?'soft':/\bhero\b/.test(t)?'white':f;
function top(html,decorate=false){const a=html.search(/<main\b/i),b=html.indexOf('</main>',a);if(a<0||b<0)return decorate?html:[];const s=html.slice(a,b),re=/<\/?(?:main|section|details)\b[^>]*>/gi,stack=[],starts=[],out=[];let m,pos=0,text='',i=0;while((m=re.exec(s))){let t=m[0];if(decorate)text+=s.slice(pos,m.index);const close=/^<\//.test(t),n=(t.match(/^<\/?([a-z]+)/i)||[])[1]?.toLowerCase();if(close){const x=stack.pop();if(n==='section'&&x==='section'&&stack.at(-1)==='main'){const q=starts.pop();if(q)out.push({...q,end:a+re.lastIndex})}}else{if(n==='section'&&stack.at(-1)==='main'){starts.push({start:a+m.index,tag:t});if(decorate)t=attr(t,'data-surface',surface(t,i++%2?'soft':'white'))}stack.push(n)}if(decorate){text+=t;pos=re.lastIndex}}return decorate?html.slice(0,a)+text+s.slice(pos)+html.slice(b):out.sort((x,y)=>x.start-y.start)}
function simplify(html,rel){if(!commercial.has(rel)||html.includes('data-service-simplified="stage77"'))return html;const s=top(html),g=s.findIndex(x=>/service-gallery-section/.test(x.tag)),k=s.findIndex(x=>/partnership-deliverables/.test(x.tag));if(g<0||k<0||g-k<3)throw Error(`${rel}: service decision path no longer matches Stage77 contract`);const move=s.slice(k+1,g),body=move.map(x=>html.slice(x.start,x.end)).join(''),lang=rel.startsWith('hu/')?'hu':rel.startsWith('de-at/')?'de':'en',label={en:'How the work is built',hu:'Hogyan épül fel a munka',de:'So entsteht die Arbeit'}[lang];for(let i=move.length-1;i>=0;i--)html=html.slice(0,move[i].start)+html.slice(move[i].end);const gallery=top(html).find(x=>/service-gallery-section/.test(x.tag));if(!gallery)throw Error(`${rel}: gallery lost during Stage77 simplification`);return html.slice(0,gallery.start)+`<details class="service-deep-dive" data-service-simplified="stage77"><summary>${label}</summary><div class="service-deep-dive-content">${body}</div></details>`+html.slice(gallery.start)}
async function walk(dir,rel=''){for(const e of await readdir(dir,{withFileTypes:true})){const f=path.join(dir,e.name),r=rel?`${rel}/${e.name}`:e.name;if(e.isDirectory()){await walk(f,r);continue}if(!e.name.endsWith('.html'))continue;let h=await readFile(f,'utf8');h=h.replaceAll(oldToken,newToken);const before=[...h.matchAll(/<(h1|h2)\b[^>]*>([\s\S]*?)<\/\1>/gi)].map(x=>plain(x[2]));h=h.replace(/<(h1|h2)\b([^>]*)>([\s\S]*?)<\/\1>/gi,(x,n,a,v)=>`<${n}${a}>${accent(v)}</${n}>`);h=top(h,true);h=simplify(h,r);const hs=[...h.matchAll(/<(h1|h2)\b[^>]*>([\s\S]*?)<\/\1>/gi)];if(JSON.stringify(before)!==JSON.stringify(hs.map(x=>plain(x[2]))))throw Error(`${r}: Stage77 changed heading text`);if(hs.some(x=>!/(bn-heading-accent|title-accent)/.test(x[2])))throw Error(`${r}: production H1/H2 lacks highlighted phrase`);if(fineArt.has(r)&&/service-deep-dive/.test(h))throw Error(`${r}: fine-art narrative must remain open`);if(h.includes(oldToken))throw Error(`${r}: stale Stage75 CSS token survived`);await writeFile(f,h,'utf8')}}
await walk(root);if(await exists(path.join(root,'portrait/index.html')))await import('./apply-service-first-principles-stage78.mjs');console.log('Stage77 applied: stable H1/H2 accents, explicit surfaces, simplified commercial service detail, open Fine Art narrative and fresh production CSS token.');
