import fs from 'node:fs';
import path from 'node:path';
const root=path.resolve(process.argv[2]||'_site/assets/css');
if(!fs.existsSync(root)){console.error(`CSS directory not found: ${root}`);process.exit(1)}
function stripComments(css){let out='',quote='',escaped=false;for(let i=0;i<css.length;i++){const ch=css[i],next=css[i+1];if(quote){out+=ch;if(escaped)escaped=false;else if(ch==='\\')escaped=true;else if(ch===quote)quote='';continue}if(ch==='"'||ch==="'"){quote=ch;out+=ch;continue}if(ch==='/'&&next==='*'){i+=2;while(i<css.length&&!(css[i]==='*'&&css[i+1]==='/'))i++;if(i>=css.length)throw new Error('Unterminated CSS comment');i++;continue}out+=ch}if(quote)throw new Error('Unterminated CSS string');return out}
function structuralBraces(css){let quote='',escaped=false,inComment=false,open=0,close=0;for(let i=0;i<css.length;i++){const ch=css[i],next=css[i+1];if(inComment){if(ch==='*'&&next==='/'){inComment=false;i++}continue}if(quote){if(escaped)escaped=false;else if(ch==='\\')escaped=true;else if(ch===quote)quote='';continue}if(ch==='/'&&next==='*'){inComment=true;i++;continue}if(ch==='"'||ch==="'"){quote=ch;continue}if(ch==='{')open++;else if(ch==='}')close++}if(inComment)throw new Error('Unterminated CSS comment');if(quote)throw new Error('Unterminated CSS string');return{open,close}}
function minify(css){const noComments=stripComments(css);let out='',quote='',escaped=false,pendingSpace=false;const tight=new Set(['{','}',':',';',',']);for(let i=0;i<noComments.length;i++){const ch=noComments[i];if(quote){out+=ch;if(escaped)escaped=false;else if(ch==='\\')escaped=true;else if(ch===quote)quote='';continue}if(ch==='"'||ch==="'"){if(pendingSpace&&out&&!tight.has(out.at(-1)))out+=' ';pendingSpace=false;quote=ch;out+=ch;continue}if(/\s/.test(ch)){pendingSpace=true;continue}if(tight.has(ch)){while(out.endsWith(' '))out=out.slice(0,-1);out+=ch;pendingSpace=false;continue}if(pendingSpace&&out&&!tight.has(out.at(-1)))out+=' ';pendingSpace=false;out+=ch}return out.trim()}
function walk(dir,files=[]){for(const entry of fs.readdirSync(dir,{withFileTypes:true})){const full=path.join(dir,entry.name);if(entry.isDirectory())walk(full,files);else if(entry.isFile()&&entry.name.endsWith('.css'))files.push(full)}return files}
const sharedStyle=path.join(root,'style.css');
const authority=path.join(root,'apple-authority-stage70.css');
const surfaceAuthority=path.join(root,'surface-authority-stage75.css');
if(!fs.existsSync(sharedStyle)||!fs.existsSync(authority)||!fs.existsSync(surfaceAuthority)){console.error('Stage70/75 production CSS authority inputs are missing.');process.exit(1)}
const authorityCss=fs.readFileSync(authority,'utf8');
const surfaceCss=fs.readFileSync(surfaceAuthority,'utf8');
if(!authorityCss.includes('STAGE70-APPLE-DESIGN-AUTHORITY:START')||!authorityCss.includes('STAGE70-APPLE-DESIGN-AUTHORITY:END')){console.error('Stage70 Apple authority markers are incomplete.');process.exit(1)}
if(!surfaceCss.includes('STAGE75-THREE-SURFACE-AUTHORITY:START')||!surfaceCss.includes('STAGE75-THREE-SURFACE-AUTHORITY:END')){console.error('Stage75 surface authority markers are incomplete.');process.exit(1)}
if(/nth-(?:child|of-type)/.test(surfaceCss)){console.error('Stage75 surface authority must not use positional nth-child/nth-of-type colouring.');process.exit(1)}
const baseStyle=fs.readFileSync(sharedStyle,'utf8');
if(baseStyle.includes('STAGE70-APPLE-DESIGN-AUTHORITY:START')||baseStyle.includes('STAGE75-THREE-SURFACE-AUTHORITY:START')){console.error('Production authorities must not be duplicated in source style.css.');process.exit(1)}
fs.writeFileSync(sharedStyle,`${baseStyle.trimEnd()}\n\n${authorityCss.trim()}\n\n${surfaceCss.trim()}\n`);
fs.unlinkSync(authority);fs.unlinkSync(surfaceAuthority);
console.log('Stage70 Apple authority and Stage75 three-surface authority composed at the end of production style.css.');
const files=walk(root);if(!files.length){console.error(`No CSS files found under ${root}`);process.exit(1)}let beforeTotal=0,afterTotal=0;for(const file of files){try{const before=fs.readFileSync(file,'utf8');const beforeStructure=structuralBraces(before);if(beforeStructure.open!==beforeStructure.close)throw new Error(`Source CSS has unbalanced structural braces (${beforeStructure.open}/${beforeStructure.close})`);const after=minify(before);const afterStructure=structuralBraces(after);if(!after)throw new Error('Minified CSS is empty');if(afterStructure.open!==afterStructure.close)throw new Error(`Minified CSS has unbalanced structural braces (${afterStructure.open}/${afterStructure.close})`);if(beforeStructure.open!==afterStructure.open||beforeStructure.close!==afterStructure.close)throw new Error(`Structural block count changed (${beforeStructure.open}/${beforeStructure.close} -> ${afterStructure.open}/${afterStructure.close})`);const output=after+'\n';fs.writeFileSync(file,output);beforeTotal+=Buffer.byteLength(before);afterTotal+=Buffer.byteLength(output);console.log(`${path.relative(root,file)}: ${Buffer.byteLength(before)} -> ${Buffer.byteLength(output)} bytes`)}catch(error){console.error(`Refusing unsafe CSS output for ${file}: ${error.message}`);process.exit(1)}}
const finalStyle=fs.readFileSync(sharedStyle,'utf8');
if(!finalStyle.includes('--bn-section-space')||!finalStyle.includes('.hero-visual-only')||!finalStyle.includes('--bn-surface-dark:#0d1b2e')||!finalStyle.includes('STAGE75-THREE-SURFACE-AUTHORITY:START')){console.error('Stage70/75 authorities did not survive production CSS composition/minification.');process.exit(1)}
const saved=beforeTotal-afterTotal;const percent=beforeTotal?saved/beforeTotal*100:0;console.log(`Production CSS: ${beforeTotal} -> ${afterTotal} bytes; saved ${saved} bytes (${percent.toFixed(1)}%).`);if(saved<1024){console.error('CSS minification saved less than 1 KiB; refusing ineffective production step.');process.exit(1)}
